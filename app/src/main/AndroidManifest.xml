<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.haanhgs.app.loginfirebase">

    <dist:module dist:instant="true" />

    <application
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name="com.haanhgs.app.loginfirebase.LoginActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="com.haanhgs.app.loginfirebase.main.MainActivity" />
        <activity android:name="com.haanhgs.app.loginfirebase.ResetActivity" />
        <activity android:name="com.haanhgs.app.loginfirebase.SignUpActivity" />

<!--        <activity-->
<!--            android:name="com.google.firebase.auth.internal.FederatedSignInActivity"-->
<!--            tools:replace="android:launchMode"-->
<!--            android:launchMode="standard" />-->

    </application>

</manifest>